---
title : "While and Maps"
description: There are multiple ways to iterate over a map in Ballerina using the `while` statement.
date: 2023-12-24
tags:
    - while
    - loop
    - iteration
    - map
    - map:keys()
    - map:toArray()
    - map:iterator()
---

There are multiple ways to iterate over a map in Ballerina using the `while` statement.

* You can iterate through the keys of a map using the `keys()` function.
* You can iterate through the values of a map using the `toArray()` function.
* You can iterate through the entries of a map using the `iterator()` function.

<!--more-->

## Example

{{< example output=false repo="https://github.com/hasithaa/Ballerina-Examples" repoPath="statements/while/while_map" default="keys.bal">}}

<!-- ## Insights

{{< tips >}} -->